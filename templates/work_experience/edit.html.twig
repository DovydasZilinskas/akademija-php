{% extends 'base.html.twig' %}

{% block title %}
	Edit Work Section
{% endblock %}

{% block body %}
	<h1>Edit Work Section</h1>
	<div class="wrapper">
		<a href="{{ path('user_profile_index') }}">Back</a>

		<div class="container">
			{{ include('work_experience/_form.html.twig', {'button_label': 'Update'}) }}

			<button type="button" class="add_item_link btn" data-collection-holder-class="duties">Add duty</button>

			{{ include('work_experience/_delete_form.html.twig') }}
		</div>
	</div>

	<script src="{{ asset('scripts/edit-work.js') }}"></script>

{% endblock %}
