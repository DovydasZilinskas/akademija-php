{% extends 'base.html.twig' %}

{% block title %}WorkExperience{% endblock %}

{% block body %}
    <h1>WorkExperience</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ work_experience.id }}</td>
            </tr>
            <tr>
                <th>Position</th>
                <td>{{ work_experience.position }}</td>
            </tr>
            <tr>
                <th>Company</th>
                <td>{{ work_experience.company }}</td>
            </tr>
            <tr>
                <th>DateFrom</th>
                <td>{{ work_experience.dateFrom ? work_experience.dateFrom|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>DateTo</th>
                <td>{{ work_experience.dateTo ? work_experience.dateTo|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>ListOne</th>
                <td>{{ work_experience.listOne }}</td>
            </tr>
            <tr>
                <th>ListTwo</th>
                <td>{{ work_experience.listTwo }}</td>
            </tr>
            <tr>
                <th>ListThree</th>
                <td>{{ work_experience.listThree }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('user_profile_index') }}">back to list</a>

    <a href="{{ path('work_experience_edit', {'id': work_experience.id}) }}">edit</a>

    {{ include('work_experience/_delete_form.html.twig') }}
{% endblock %}
